# 森林氧吧·AI智慧康养系统 —— 需求分析文档

## 1. 引言

### 1.1 项目背景
“森林氧吧·AI智慧康养系统”是为一家森林康养基地量身打造的一体化数字管理平台。系统覆盖客户、护工、管理员三类角色，集预约管理、人员管理、项目管理、环境监测及AI咨询于一体，旨在提升康养服务的运营效率与客户体验，实现线上线下融合的智慧康养服务。

### 1.2 目标用户
- **系统管理员**：负责日常运营监控、人员与项目管理。
- **护工**：查看个人排班、服务详情及项目信息。
- **客户（用户）**：浏览项目、预约服务、查看个人健康档案及享受AI咨询。

### 1.3 设计原则
- **角色分离**：三类角色入口独立，权限清晰。
- **数据驱动**：所有操作实时同步数据库，保证一致性。
- **易用性与视觉舒适**：面向中老年群体，界面应简洁、大字体、高对比度，并融入森林康养风格。
- **安全合规**：个人信息保护、登录验证、数据备份。

---

## 2. 功能需求（按模块）

### 2.1 登录与注册（公共模块）
| 功能点 | 描述 |
|--------|------|
| 登录 | 使用手机号（11位）作为账号，密码为8位数字。验证手机号存在性、格式、密码正确性及护工在职状态。失败时给出明确提示，成功跳转对应角色主页。支持“去注册”跳转。 |
| 注册 | 选择角色（客户/护工），填写昵称、年龄、手机号、密码。验证手机号11位、密码8位数字、年龄0-150及唯一性。系统自动分配6位编号（0开头管理员，1开头护工，2开头客户）。注册成功提示并跳转登录。 |
| 界面布局 | 登录/注册页位于右侧，左侧预留康养院实景图片位（静态或轮播）。 |

### 2.2 管理员界面
顶部导航：数据看板、客户管理、护工管理、项目管理。

#### 2.2.1 数据看板
- 展示统计卡片：客户总数、护工总数、项目总数、预约总数。
- 环境数据模拟：园区内温度、湿度、空气质量、负氧离子浓度（在合理范围内动态更新）。

#### 2.2.2 客户管理
- 列表展示：编号、姓名、年龄、手机号、操作（查看）。
- 查看详情弹窗：显示客户基本信息（姓名、年龄、手机号、忌口、疾病史、偏好）及该客户的项目列表（含日期【今/明/后天】+时段、项目名、护工名及编号），底部显示项目总数；可切换查看该客户的历史预约（已完成/已取消）。

#### 2.2.3 护工管理
- 列表展示：编号、姓名、年龄、手机号、薪资、可服务项目、在职/离职状态（可编辑切换）。
- 操作（编辑）：点击弹出新页面，按时间顺序（今/明/后天 + 每日4时段）列表显示每个时段的服务内容（项目名+客户姓名+客户编号）或“空闲”。离职人员不可分配新服务。

#### 2.2.4 项目管理
- 列表展示：编号、项目名称、地点、费用、同一时间段最大容量（人）、状态（进行/停用）、操作（编辑/查看）。
- 添加新项目：输入所有列表字段，自动生成编号。
- 查看：跳转新界面，显示该项目12个时段（今/明/后天 × 4时段）的预约/容量比例（如 6/8）。
- 编辑：跳转编辑页，可修改项目具体描述（见附录一）及其他必要信息（除编号外均允许修改），停用后项目不可预约。

### 2.3 护工界面
顶部导航：个人中心、服务时间状态、项目总览。

#### 2.3.1 个人中心
显示工号、姓名、性别、年龄、手机、薪资（只读）。

#### 2.3.2 服务时间状态
按“今/明/后天”三大块，每块含4个时段，每个时段显示时间、状态（空闲/服务中），若服务中则显示客户名、项目、忌口、疾病史、偏好（由系统自动分配排班，护工不可更改）。已完成的服务自动转入本人“服务历史”仅供回顾（不可修改）。

#### 2.3.3 项目总览
同管理员的项目列表（只读），查看详情同管理员查看界面（含项目描述和时段容量）。

### 2.4 客户（用户）界面
顶部导航：个人中心、数据看板、我的项目、项目总览、AI咨询。

#### 2.4.1 个人中心
展示编号、手机号（只读），其余字段（姓名、年龄、忌口、疾病史、偏好）可编辑，编辑后同步数据库。

#### 2.4.2 数据看板
同管理员数据看板（只读展示）。

#### 2.4.3 我的项目
按今/明/后天及每日4时段列表。若已预约，显示项目名、地点、费用、护工名及编号，并提供“取消”操作，取消后显示“未预约”并提示成功；若未预约显示“未预约”。已完成的预约自动归档，可在“历史记录”中按日期倒序查看（含已取消项），供健康档案回顾。

#### 2.4.4 项目总览
同护工项目总览，增加“预约”功能。按时间段预约时，需检查该用户该时段是否已预约（若已预约则提示），并从该时段内空闲护工中随机分配一名，预约成功同步数据库。

#### 2.4.5 AI咨询
接入豆包API，提供常见问题快捷入口，界面风格活泼有趣，便于中老年用户使用。

### 2.5 全局顶部标题栏
- 左侧显示“森林氧吧”标题。
- 旁边有地图按钮，点击弹出/收起园区地图图片。
- 最右侧显示当前登录用户名称及“退出登录”按钮，点击退出返回登录页。

---

## 3. 非功能性需求

### 3.1 性能需求
| 指标 | 要求 |
|------|------|
| 页面加载时间 | 首屏加载 ≤ 2秒（含图片），列表渲染 ≤ 1秒。 |
| 并发用户数 | 支持至少 200 个并发登录/操作，响应时间不恶化。 |
| 数据库操作 | 增删改查操作响应时间 ≤ 500ms（百万级数据量下）。 |
| 环境数据刷新 | 数据看板环境指标每 5~10 秒模拟更新一次，不造成明显卡顿。 |

### 3.2 可用性与易用性
- **适老化设计**：字体大小 ≥ 16px，关键按钮尺寸 ≥ 44×44pt，高对比度色差（WCAG AA标准）。
- **操作反馈**：所有提交、删除、编辑等操作均有明确的成功/失败提示（Toast或弹窗）。
- **导航清晰**：顶部导航固定，面包屑辅助（管理员/护工/客户均保持一致的层级结构）。
- **容错提示**：所有输入框均有即时格式校验（手机号、密码、年龄），错误信息具体易懂。

### 3.3 安全性
- **身份认证**：登录密码采用加密存储（如bcrypt），传输使用HTTPS。
- **权限控制**：三类角色严格隔离，管理员不可查看客户密码，护工和客户不可访问管理后台。
- **数据隐私**：客户忌口、疾病史等敏感信息仅本人及其负责护工可见，管理员可查看但不可随意导出。
- **防暴力破解**：登录失败超过5次，账号锁定15分钟。
- **退出机制**：会话超时（30分钟无操作）自动退出。

### 3.4 可维护性与可扩展性
- **前后端分离**：前端采用Vue/React框架，后端提供RESTful API，便于后期迭代。
- **数据库设计**：遵循第三范式，预留扩展字段（如未来增加项目类型、多基地支持）。
- **日志记录**：关键操作（登录、注册、预约、取消、编辑）记录操作日志，便于审计。
- **配置管理**：环境数据模拟阈值、时段定义、项目清单等支持后台配置（或配置文件），无需硬编码。

### 3.5 可靠性
- **数据一致性**：预约/取消等并发操作使用数据库事务或乐观锁，防止超卖、重复预约。
- **异常处理**：网络超时或服务端错误时，前端统一捕获并给出友好提示，不导致页面崩溃。
- **数据备份**：每日自动备份数据库，备份保留30天。

### 3.6 兼容性与环境
- **浏览器支持**：主流浏览器（Chrome、Edge、Safari、Firefox）最近两个大版本。
- **响应式设计**：最低支持 1366×768 分辨率，同时适配平板（1024×768）基本可用。
- **移动端适配**：为手机端提供简化视图（可选，但本次需求中主桌面端）。

### 3.7 可测试性
- 模块解耦，单元测试覆盖核心逻辑（预约算法、编号生成、时段冲突检测）。
- 提供模拟测试环境，可预设数据（如空闲护工、项目容量）进行自动化测试。

### 3.8 法律与合规
- 遵循《个人信息保护法》，用户注册即同意隐私政策，数据仅用于康养服务。
- 年龄、疾病等健康数据收集需明确告知目的，并获得用户授权。

---

## 4. 数据字典（简要）

| 实体 | 关键字段 |
|------|----------|
| 用户（客户/护工） | 编号（6位，含角色标识）、手机号（唯一）、姓名、昵称、年龄、密码（加密）、角色、状态（在职/离职，仅护工）、薪资、可服务项目（护工）、忌口/疾病史/偏好（客户） |
| 项目 | 编号（自动）、名称、地点、费用、最大容量、状态（进行/停用）、详细描述（含附录一内容） |
| 预约记录 | 预约编号、客户编号、护工编号、项目编号、服务日期（绝对日期，存储用）、时段（0-3）、状态（已预约/已完成/已取消）、预约时间、取消时间（如有） |
| 时段 | 固定四个（0~3），分别对应08:30-10:00, 10:30-12:00, 14:00-15:30, 16:00-17:30 |

> 说明：业务上开放预约窗口为“今/明/后天”，但**存储层必须采用绝对服务日期（serviceDate，YYYY-MM-DD）**，“今/明/后天”仅是查询与展示视图，不承担存储职责（详见 4.1）。

### 4.1 预约数据存储与历史记录设计（V1.1 新增）

**背景与结论**：系统预约窗口仅覆盖未来三天，历史预约记录仍**必须保留**。若只存相对日期并在预约过期后删除记录，将无法支撑客户健康档案延续、护工服务经历统计、管理员运营/审计分析（对应 3.3、3.4），也无法满足合规追溯。因此采用“**状态流转、不物理删除**”的存储策略：预约时间一到，记录转入“已完成”，长期保留。

1. **绝对服务日期与业务窗口分离**
   - 预约以 `serviceDate`（YYYY-MM-DD）绝对日期落库；每次仅允许创建 `serviceDate ∈ [今天, 今天+2]` 的预约。
   - “今/明/后天 × 每日 4 时段”仅作为排班与客户端展示窗口；超出该窗口的记录自动成为历史数据。
   - 若业务升级为长期预约（如提前一周），仅放宽窗口过滤条件即可，数据模型无需变更。

2. **存储方案（本系统采用方案 A）**
   - 方案 A（采用）：单一预约记录集 + 状态流转（已预约 → 已完成 / 已取消），记录不因过期而删除。适合本系统规模，数据源唯一、逻辑简单一致。
   - 方案 B（备选）：操作表只放未来 3 天，过账后搬入只读归档表 `booking_history`。适用于数据量大、操作表需保持轻量的场景。
   - 方案 C（不采用）：另设只读分析库/日志表用于长期报表。适用于大规模、跨基地的长期分析场景。

   采用方案 A：预约记录与历史记录存放于**同一数据源**，历史查询即对同一表加过滤条件，避免两份数据不一致。

3. **过账（Rollover）规则**
   - 系统初始化或每日定时任务中，将 `status='已预约'` 且 `serviceDate < 今天` 的记录统一置为“已完成（未赴约如实标记）”。
   - 已“完成/取消”的记录不再占用时段容量，也不参与护工新排班分配。
   - 状态机：`已预约 → 已完成`（到期过账）、`已预约 → 已取消`（客户主动取消）。
   - 旧数据迁移：存量相对日期 `day（0/1/2）` 依当时真实日期一次性换算为 `serviceDate`，并做兼容迁移。

4. **历史记录查询入口（对应各角色功能）**
   - 客户：“我的项目”提供“历史记录”视图，按日期倒序查看本人已完成/已取消的预约（项目、时段、护工）。
   - 护工：“服务时间状态”可回顾本人已完成的服务记录（仅信息查询，不可修改）。
   - 管理员：客户详情可查询该客户全部历史预约，用于运营统计与审计。
   - 隐私约束：历史记录同样遵守 3.3——客户健康敏感信息仍限本人、负责护工及管理员可见。

5. **备份与保留**：历史记录不因备份过期而删除；每日快照备份保留 30 天（对应 3.5 数据备份）。

---

## 5. 附录（引用说明）
本系统所有服务项目的详细描述（地点、时长、流程、费用、容量、适合人群、禁忌）均依据《森林氧吧·AI智慧康养系统服务项目清单》（附录一）实现，作为项目管理的核心数据源，其内容在管理员编辑时允许修改，但初始数据应完整录入。

---

**文档版本**：V1.1  
**编写日期**：2026年9月4日  
**编写人**：
**V1.1 修订说明**：新增“预约数据存储与历史记录设计”（§4.1），明确预约以绝对服务日期（serviceDate）落库、历史记录采用“状态流转、不物理删除”策略并保留 30 天备份；同步补充客户/护工/管理员的历史记录查询功能点（§2.2.2、§2.3.2、§2.4.3）及数据字典字段修订（§4）。
